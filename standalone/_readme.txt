This is the execution folder of the project.

Initially you have to ensure all dependencies are present.
Therefore execute
  _copy-deps.cmd
it will create a folder named "jar" and copy all dependencies into it.
For copy-deps to work, Apache Maven must be installed and in the PATH
